<Window x:Class="Multron_Push_To_Talk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Multron Push to Talk v1.2 beta"
        Height="500" Width="800"
        Background="#1E1E2F"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="14"
        Loaded="Window_Loaded"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="False">
    

    <Window.Resources>
        
        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="#2A2A3D" 
                                BorderBrush="#444" 
                                BorderThickness="1" 
                                CornerRadius="5">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style> 
        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#3B83F6"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3B83F6"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
      
        <Grid Height="40" Background="#2A2A3D" MouseDown="TopBar_MouseDown" Margin="0,0,0,460">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Multron Push to Talk v1.3 beta" 
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"
                       Foreground="White"
                       FontWeight="Bold"/>

            <Button Grid.Column="1"
                    Content="–"
                    Click="Minimize_Click"
                    Style="{StaticResource WindowControlButton}"/>

            <Button Grid.Column="2"
                    Content="✕"
                    Click="Exit_Click"
                    Style="{StaticResource WindowControlButton}" HorizontalAlignment="Right" Margin="0,5,0,5" Width="40"/>
        </Grid>
 
        <Grid Margin="30,50,30,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
 
            <TextBlock Name="PushToTalkText"
                       Text="Select Key or MouseHook for Push to Talk"
                       Foreground="White"
                       FontSize="20"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,20,0,20"/>

           
            <StackPanel Grid.Row="1" Margin="0,10,0,0" HorizontalAlignment="Center" Width="400">
                <TextBlock Text="Shortcut Key / Mouse Button" Foreground="White" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox Name="KeySelectionTextBox"
                         Style="{StaticResource ModernTextBox}"
                         Text="Press a key or click a mouse button..."
                         IsReadOnly="True"
                         KeyDown="KeySelectionTextBox_KeyDown555"/>
                        

                <TextBlock Text="Select Microphone" Foreground="White" FontWeight="Bold" Margin="0,20,0,5"/>
                <ComboBox Name="MicrophoneComboBox"
                          Height="35"
                          Background="#2A2A3D"
                          Foreground="Black"
                          BorderBrush="#444"
                          BorderThickness="1"
                          SelectionChanged="MicrophoneComboBox_SelectionChanged"/>
            </StackPanel>
 
            <Grid Grid.Row="2" Margin="0,30,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
 
                <StackPanel Grid.Column="0" Margin="0,0,10,0" VerticalAlignment="Top">
                    <CheckBox Name="EnableTrayIconCheckBox"
                              Content="Enable Tray Icon"
                              Foreground="White"
                              Margin="0,0,0,10"
                              Checked="EnableTrayIconCheckBox_Checked"
                              Unchecked="EnableTrayIconCheckBox_Unchecked"/>

                    <CheckBox x:Name="Hider"
                              Content="Make X Button a Hider"
                              Foreground="White"
                              Checked="MakeHider_Checked"
                              Unchecked="MakeCloser_Unchecked"/>
                </StackPanel>

              
                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <Button Name="StopListen"
                            Content="Stop Listening"
                            Style="{StaticResource ModernButton}"
                            Width="150"
                            Height="40"
                            Click="Button_Click"
                            Margin="0,0,0,10"/>

                    <Button Content="Reset"
                            Style="{StaticResource ModernButton}"
                            Width="150"
                            Height="40"
                            Click="Button_Click_1"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
